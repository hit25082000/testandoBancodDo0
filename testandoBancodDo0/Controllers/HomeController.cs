using Microsoft.AspNetCore.Mvc;

/*namespace testandoBancodDo0.Controllers
{
    public class HomeController : Controller
    {
        public async Task<IActionResult> MinhaPagina()
        {
            // Carrega a sessão antes de acessar os valores
            await HttpContext.Session.LoadAsync();

            // Obtém o nome do usuário da sessão
            string nomeUsuario = HttpContext.Session.GetString("UserName");

            // Define o nome do usuário na ViewBag para ser acessível na visão
            ViewBag.NomeUsuario = nomeUsuario;

            return View();
        }
    }


}
*/